#include <stdlib.h>
#include "main.h"
/**
 * strtow - Splits a string into an array of words.
 * @str: The input string to be split.
 *
 * Return: Pointer to an array of words (strings) or NULL if str is NULL or empty.
 */
char **strtow(char *str);

