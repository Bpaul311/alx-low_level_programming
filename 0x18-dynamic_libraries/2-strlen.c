#include "main.h"
/**
 * _strlen - Computes the length of a string.
 * @s: The string to compute the length of.
 *
 * Return: Returns the length of the string.
 */
int _strlen(char *s)
{
    /* Empty function, implementation goes here */
}
